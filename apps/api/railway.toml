[build]
builder = "NIXPACKS"

[deploy]
startCommand = "pnpm --filter @panccre/api start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
