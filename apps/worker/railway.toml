[build]
builder = "NIXPACKS"
buildCommand = "pnpm --filter @panccre/worker build"

[deploy]
startCommand = "PYTHONPATH=/app/src python3 -m panccre.worker.main"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
