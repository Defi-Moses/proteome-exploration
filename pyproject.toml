[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "panccre"
version = "0.1.0"
description = "Pangenome-aware regulatory element registry scaffolding"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
